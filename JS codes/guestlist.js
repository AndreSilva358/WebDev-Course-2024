let guestName = prompt ("What is your name?");
let guestList = [Angela, Jack, Pam, Arthas, Vancliff, LamarJackson];
if (guestList.includes (guestName)) {
    alert ("Welcome!")
}
else {
    alert ("Sorry. Maybe next time.")
}
